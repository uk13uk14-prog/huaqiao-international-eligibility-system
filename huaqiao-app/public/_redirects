# SPA fallback is configured in repo-root wrangler.toml:
#   [assets] not_found_handling = "single-page-application"
#
# Do NOT add `/* /index.html 200` here — Cloudflare Workers Static Assets
# treats that as an infinite redirect loop (error 100324).
