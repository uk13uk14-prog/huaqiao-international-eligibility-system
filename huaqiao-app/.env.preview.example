# Cloudflare Workers Builds / Preview 构建环境变量示例
# 正式生产构建请使用仓库内 huaqiao-app/.env.production
#
# 禁止填写：localhost / 127.0.0.1 / 192.168.* / 100.97.* / 其它仅 Tailscale 可达地址

# 免费版后端 API 根地址（HTTPS，无尾斜杠）
VITE_API_BASE=https://api.guoqiaoplan.com

# SaaS Pro 后端 API 根地址（HTTPS，无尾斜杠）
# 与正式 API 同域时保持相同 origin；路径仍为 /api/...，勿写成 /saas-api/saas-api
VITE_SAAS_API=https://api.guoqiaoplan.com
