# Cloudflare Pages Preview 构建环境变量示例
# 在 Cloudflare Dashboard → Pages → Settings → Environment variables → Preview 中配置
# 不要将此文件复制为 .env 并提交到仓库
#
# STAGING_API_REQUIRED=YES
# 未配置公网 Staging API 前：H5 静态页可打开，业务数据请求会失败并降级为空列表
# 禁止填写：localhost / 127.0.0.1 / 192.168.* / 100.97.* / 其它仅 Tailscale 可达地址

# 免费版后端 API 根地址（HTTPS，无尾斜杠）
# 示例：https://staging-api.example.com
# 留空则构建产物请求相对路径 /api（仅适用于与 H5 同域部署或已配 Pages Functions/反代）
VITE_API_BASE=

# SaaS Pro 后端 API 根地址（HTTPS，无尾斜杠）
# 示例：https://staging-saas-api.example.com
# 留空则使用相对路径 /saas-api（仅适用于与 H5 同域或反向代理同域）
VITE_SAAS_API=
