# Secondary config when deploying from huaqiao-app/ cwd (local APK / legacy Actions).
# Cloudflare Git Workers Builds uses the REPO ROOT wrangler.toml instead.
#
# Prefer: cd repo-root && npm run deploy:h5

name = "huaqiao-international-eligibility-system"
compatibility_date = "2025-09-04"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
