name = "guoqiao-api"
main = "src/index.js"
compatibility_date = "2025-09-04"
workers_dev = true

routes = [
  { pattern = "api.guoqiaoplan.com", custom_domain = true }
]

# Set at deploy time (do not commit ephemeral tunnel URLs):
#   npx wrangler secret put BACKEND_ORIGIN
# or [vars] BACKEND_ORIGIN = "https://...."
